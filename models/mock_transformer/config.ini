
# Jellybean Model Configuration
# Model: mock_transformer
# This is the Triton-style per-model config for the mock_transformer model.

name=transformer
backend=torchscript
version=1
device=cpu

# Batching
max_batch_size=4
max_batch_delay_us=1000

# Input tensor shape for a single request
input_shape=1,128,512
